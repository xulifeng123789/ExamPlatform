字节转换工具类(ByteUtils)
	十六进制 转换 byte[]
	byte[] 转 16进制字符串
	

日期工具类(DateUtils)
	根据日期格式字符串解析日期字符串 
	根据单位字段比较两个日期
	根据单位字段比较两个时间
	获得当前的日期毫秒
	获得当前的时间戳
	yyyy-MM-dd 当前日期
	yyyy-MM-dd 传入日期
	yyyyMMdd 传入日期
	yyyy-MM-dd 传入的时间戳
	HH:mm:ss 当前时间
	得到时间戳格式字串
	得到长日期格式字串
	得到短日期格式字串
	计算 second 秒后的时间
	计算 minute 分钟后的时间
	计算 hour 小时后的时间
	得到day的起始时间点
	得到day的终止时间点.
	计算 day 天后的时间
	得到month的终止时间点.
	取得两个日期之间的日数
	返回java.sql.Timestamp型的SYSDATE
	利用缺省的Date格式(YYYY/MM/DD)转换String到java.sql.Timestamp
	以YYYY/MM/DD HH24:MI:SS格式返回系统日期时间
	根据指定的Format转化java.util.Date到String
	利用指定SimpleDateFormat instance转换java.util.Date到String
	转换java.sql.Timestamp到String，格式为YYYY/MM/DD HH24:MI
	根据指定的格式转换java.sql.Timestamp到String
	得到当前日期的星期
	Timestamp 格式转换成yyyy-MM-dd timestampToSql(Timestamp 格式转换成yyyy-MM-dd)
	判断一个时间是否在某个时间区间内
	判断时间是否在制定的时间段之类
	得到当前周起始时间
	得到当前周截止时间
	得到当月起始时间
	得到当前年起始时间
	得到当前年最后一天
	取得月天数
	取得月第一天
	取得月最后一天
	取得季度第一天
	取得季度最后一天
	取得季度月
	1 第一季度 2 第二季度 3 第三季度 4 第四季度
	字符串转date
	判断输入日期是一个星期中的第几天(星期天为一个星期第一天)
	当前时间的前几天，并且以例如2013/12/09 00:00:00 形式输出
	判断开始时间和结束时间，是否超出了当前时间的一定的间隔数限制 如：开始时间和结束时间，不能超出距离当前时间90天
	判断开始时间和结束时间，是否超出了当前时间的一定的间隔数限制, 时间单位默认为天数 如：开始时间和结束时间，不能超出距离当前时间90天
	传入时间字符串及时间格式，返回对应的Date对象
	取季度
	取得通用日期时间格式字符串
	获取当日的日期格式串
	获取当前时间格式串
	取得指定日期格式的字符串
	获取昨日的日期格式串
	判断当前时间是否在一定的时间范围内
	字符转日期
	根据传入的数字，输出相比现在days天的数据
	日期最大时间
	日期最小时间
	月的最后一天
	月的第一天
	上月第一天
	上月最后一天
	两个日期相关天数
	判断两个时间是不是在一个周中
	对时间进行格式化

文件工具类(FileUtils)
	传入文件夹路径，该方法能够实现创建整个路径
	该方法用来判断文件夹是否存在，如果不存在则创建，存在则什么都不做

隐藏各类敏感信息工具类(MaskUtils)
	隐藏手机号
	隐藏邮箱信息
	隐藏卡号信息
	隐藏身份证号码
	隐藏银行卡号码
	

数字格式化工具类(NumberFormatUtils)
	格式表达式
	格式化
	解析

Object值转为不同类型的数值(NumberUtil)
	判断对象值是否为纯数字组成(如:001234,7899),只有纯数字的值才能转Long或Integer
	判断对象值是否为数字,包含整数和小数(如:001234,7899,7.99,99.00)
	Object对象转BigDecimal
	Object对象转Double
	Object对象转Long
	Object对象转Integer
	

Bean属性访问工具(PropertyUtil)
	属性注入
	获取对象属性值
	获取对象的所有属性描述
	获取对象属性描述
	获取CLASS属性类型
	获取CLASS所有属性名称
	
行政区域工具类(ReginUtil)
	获取省份的代码与名称映射
	获取市级代码与名称映射
	根据省份代码获取省份名称
	根据省份名称获取代码
	根据市级代码获取市级名称
	根据市级名称获取代码
	键值去首尾空格
	
资源文件工具类(ResourceUtil)  ResourceBundle：读取资源属性文件（properties）

线程工具(ThreadUtil)--->本地线程的存取

UUID随机数生成工具(UUIDUtil)---checked
	返回一个定长的随机字符串(只包含数字)
	返回一个定长的随机字符串(只包含大小写字母、数字)
	返回一个定长的随机字符串(包含大小写字母、数字、特殊符号)
	返回一个定长的随机纯字母字符串(只包含大小写字母)
	返回一个定长的随机纯小写字母字符串(只包含小写字母)
	返回一个定长的随机纯大写写字母字符串(只包含大写字母)
	生成一个定长的纯0字符串
	根据数字生成一个定长的字符串，长度不够前面补0
	八位数字+字母+特殊字符随机密码生成
	
	